# www

## Description

A very simple web page/application for demonstrating the `oembeddings_validate.wasm` WebAssembly binary.

## Important

WASM binaries are not bundled with this package. Run the `make wasmjs` target from the root folder.